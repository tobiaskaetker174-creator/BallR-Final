{
  "buildCommand": "cd artifacts/mobile && npx expo export --platform web",
  "outputDirectory": "artifacts/mobile/dist",
  "installCommand": "npm i -g pnpm@9 && pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
